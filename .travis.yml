sudo: required
language: python

services:
  - docker

script:
  - docker run -v ${PWD}:/src -v ${PWD}:/output:Z centos:centos7 /src/ci/build.sh

install: true

deploy:
  provider: releases
  github_token: ${GITHUB_API_TOKEN}
  file:
    - "output/alice-system-deps-$TRAVIS_TAG-1.x86_64.rpm"
  skip_cleanup: true
  on:
    tags: true
